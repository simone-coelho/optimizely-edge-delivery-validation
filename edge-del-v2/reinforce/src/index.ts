// Public entry — exports the shared types so worker, companion, and
// harness can import them without depending on the browser companion
// source.
export type { Op, OpType, VariationManifest, Case } from './types';
